import rentAVehicle.*
 
test "Car efficiency" {
	assert.equals(18, new Car(360, 2).efficiency())
} 

test "Moto efficiency" {
	assert.equals(40, new Moto(1000).efficiency())
}

test "Bike efficiency" {
	assert.equals(24, new Bike(20).efficiency())
}